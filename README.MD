# 🏆 SSPT - School System for Programming Tournaments

Платформа для проведения школьных соревнований по программированию с автоматической проверкой решений, системой рейтинга и удобным веб-интерфейсом.

## 📋 Содержание

- [Особенности](#-особенности)
- [Технологии](#-технологии)
- [Быстрый старт](#-быстрый-старт)
- [Структура проекта](#-структура-проекта)
- [Настройка контестов и задач](#-настройка-контестов-и-задач)
- [Кастомный чекер](#-кастомный-чекер)
- [Управление пользователями](#-управление-пользователями)
- [API и работа системы](#-api-и-работа-системы)
- [Часто задаваемые вопросы](#-часто-задаваемые-вопросы)

## ✨ Особенности

- **Гибкая структура**: от 1 до 5 контестов, в каждом от 1 до 5 задач (A, B, C, D, E)
- **Автоматическая проверка решений** на 100 тестах
- **Система оценивания**: 1 балл за каждый пройденный тест
- **Режимы тестирования**:
  - *Последовательный*: остановка после первой ошибки
  - *Независимый*: выполнение всех тестов независимо
- **Кастомные чекеры** для сложных проверок вывода
- **Docker-контейнеризация** для простого развертывания

## 🛠 Технологии

- **Backend**: Python 3.x + Flask
- **Frontend**: HTML/CSS
- **База данных**: JSON-файл (users_data.json)
- **Контейнеризация**: Docker

## 🚀 Быстрый старт

### Запуск через Docker

1. **Клонируйте репозиторий**:
   ```bash
   git clone <https://github.com/Mishcsas/SSPT>
   cd SSPT
   ```

2. **Соберите Docker-образ**:
   ```bash
   docker build -t tournament-platform .
   ```

3. **Запустите контейнер и войдите в него**:
   ```bash
   docker run -it -p 0.0.0.0:5000:5000 --name tournament tournament-platform
   ```

4. **Добавьте первого участника**:
   ```bash
   python admin.py
   add user 123
   ```

5. **Откройте браузер**:
   ```
   http://{ip_сервера}:5000
   ```

### Полезные команды Docker

```bash
# Просмотр логов
docker logs -f tournament

# Остановка контейнера
docker stop tournament

# Запуск остановленного контейнера
docker start tournament

# Удаление контейнера
docker rm tournament

# Войти в контейнер
docker exec -it tournament bash
```

## 📁 Структура проекта

```
SSPT/
│
├── tasks/                          # Все данные по контестам и задачам
│   ├── contest1/                   # Контест #1 (обязательно с contest1)
│   │   ├── config.json             # Настройки контеста
│   │   ├── A/                      # Задача A
│   │   │   ├── config.json         # Настройки задачи
│   │   │   ├── problem.txt         # Условие (HTML)
│   │   │   ├── test/               # Входные тесты (1.txt...100.txt)
│   │   │   └── ans/                # Ожидаемые результаты (1.txt...100.txt)
│   │   ├── B/                      # Задача B (опционально)
│   │   └── ...                     # Задачи C, D, E (опционально)
│   ├── contest2/                   # Контест #2 (опционально)
│   └── ...                         # Контесты #3-5 (опционально)
│
├── templates/                      # HTML-шаблоны
│   ├── index.html                  # Главная страница
│   ├── contest.html                # Страница контеста
│   ├── task.html                   # Страница задачи
│   ├── login.html                  # Страница авторизации
│   └── 404.html                    # Страница ошибки
│
├── test/                           # Временные файлы решений
│
├── admin.py                        # Управление пользователями
├── checker.py                      # Модуль проверки решений
├── generator.py                    # Вспомогательные функции для генерации тестов
├── main.py                         # Основной сервер (Flask)
├── users_data.json                 # База данных пользователей
├── requirements.txt                # Зависимости Python
└── dockerfile                      # Docker-конфигурация
```

**Важно**: 
- Контесты должны идти по порядку: если есть `contest2`, обязательно должен быть `contest1`
- Задачи должны идти по порядку: если есть `B`, обязательно должна быть `A`
- Можно создать 1 контест с 1 задачей, остальное необязательно

## 🔧 Настройка контестов и задач

### Создание контеста

1. Создайте папку `tasks/contest1/` (обязательно с единицы)
2. Добавьте файл `config.json`:

```json
{
    "contest_name": "Введение в алгоритмы",
    "contest_info": "Первый контест для знакомства с платформой",
    "A_name" : "A. Сумма",
    "A_info" : "Даны два числа, выведите их сумму",
    "A_difficulty" : "easy"
}
```
`A_name, A_info, A_difficulty` - заполняется для каждой задаче которая есть в контесте
`A_difficulty` = (easy, medium, hard)

### Добавление задачи

1. Внутри папки контеста создайте папку `A` (обязательно с A)
2. Добавьте необходимые файлы:

#### `config.json` - настройки задачи

```json
{
    "name": "Сумма двух чисел",
    "time_limit": "1",
    "memory_limit": "256",
    "sample_in_1": "2 2",
    "sample_out_1": "4",
    "sample_in_2": "3 5",
    "sample_out_2": "8",
    "use_checker": false,
    "independent_tests": false
}
```

| Поле | Обязательное | Описание |
|------|-------------|----------|
| `name` | ✅ | Название задачи |
| `time_limit` | ✅ | Ограничение по времени (секунды) |
| `memory_limit` | ✅ | Ограничение по памяти (мегабайты) |
| `sample_in_1`, `sample_out_1` | ✅ | Пример для отображения участникам |
| `sample_in_2`, `sample_out_2` | ✅ | Второй пример |
| `use_checker` | ❌ | Использовать кастомный чекер (по умолчанию `false`) |
| `independent_tests` | ❌ | Независимое тестирование (по умолчанию `false`) |

#### `problem.txt` - условие задачи

HTML-код с описанием задачи. Пример:
```html
На вход подаются два целых числа A и B. Выведите их сумму.<br>
Гарантируется что A < 10<sup>5</sup>, B < 10<sup>5</sup>
```

#### Подготовка тестов

Создайте папки `test/` и `ans/` с файлами от `1.txt` до `100.txt`:

**test/1.txt** (входные данные):
```
5 3
```

**ans/1.txt** (ожидаемый результат):
```
8
```

**Важно**: 
- Всегда должно быть ровно **100 тестов**
- Файлы нумеруются с 1
- Если задача использует чекер (`use_checker: true`), в `ans/` помещается то, что должен вернуть чекер (например, `1` или `OK`)

### Режимы тестирования

#### Обычный режим (`independent_tests: false`)
- Тесты выполняются последовательно
- При первой ошибке тестирование прекращается
- Участник получает баллы только за пройденные тесты до ошибки

#### Независимый режим (`independent_tests: true`)
- Все тесты выполняются независимо
- Даже при ошибке в одном тесте, остальные продолжают проверяться
- Участник получает баллы за все пройденные тесты

## 🔧 Кастомный чекер

Чекер позволяет гибко проверять вывод участника, когда простое сравнение строк не подходит.

### Как добавить чекер

1. Напишите `checker.py` и добавьте его в папку задачи

2. В `config.json` задачи установите:
   ```json
   "use_checker": true
   ```

3. В папке `ans/` для каждого теста укажите ожидаемый результат чекера

### Как работает чекер

`checker.py` обрабатывает вывод программы участника и выводит результат в консоль, после чего результат записывается в `test/{id}_out.txt`, дальше `основной чекер` сравнивает вывод с содержимым `ans/{i}.txt`.

#### Примеры использования чекера:

**1. Игнорирование пробелов и регистра:**
- Вывод участника: `"  Hello  World  "`
- Чекер приводит к: `"helloworld"`
- В `ans/1.txt`: `"helloworld"`

**2. Проверка с погрешностью (задачи с плавающей точкой):**
- Вывод участника: `"0.33333333"`
- Чекер сравнивает с абсолютной/относительной погрешностью
- В `ans/1.txt`: `"0.333"` (ожидаемое значение)

**3. Решения с нескольми ответами:**
- Задача: вывести 3 числа произведение которых будет равно n
- Чекер проверяет корректность ввода, перемножает числа,
  и возвращает результат произведения
- В `ans/1.txt`: `n`
- Если правильный ответ чекер тоже вернет `n`


### Пример реализации чекера

```python
# checker.py
import sys

user_input = sys.stdin.read()
user_input = user_input.lower()
user_input = user_input.split()

if (len(user_input) == 3 and user_input[0].isdigit() and user_input[1].isdigit() and user_input[2].isdigit()):
    res = 1
    for i in range(3):
        res *= int(user_input[i])
    print(res)
else:
    print(0)
```

## 👥 Управление пользователями

Скрипт `admin.py` - основной инструмент для управления данных участников,
запускает бесконечный цикл и считывает команды.
`Не стоит его запускать во время работы сервера т.к. есть риск потери данных участников`.

### Команды

| Команда | Синтаксис | Описание | Пример |
|---------|-----------|----------|---------|
| `add` | `add <user> <password>` | Добавить участника | `add ivan 12345` |
| `del` | `del <user>` | Удалить участника | `del ivan` |
| `clear` | `clear <user>` | Очистить результаты участника | `clear ivan` |
| `delall` | `delall` | Удалить всех участников | `delall` |
| `clearall` | `clearall` | Очистить результаты всех участников | `clearall` |
| `top` | `top` | Сформировать рейтинг в top.txt | `top` |
| `end` | `end` | Закончить ввод команд | `end` |

### Формат базы данных (`users_data.json`)

```json
{
  "ivan": {
    "password": "12345",
    "contest1A": 100,
    "contest1B": 85,
    "contest2A": 100
  },
  "petr": {
    "password": "qwerty",
    "contest1A": 100
  }
}
```

- Ключ задачи: `contest{номер}{буква}` (например, `contest3C`)
- Баллы: от 0 до 100 (1 балл за тест)

## 🔧 API и работа системы

### Процесс проверки решения

1. Участник отправляет решение через веб-форму
2. Файл с уникальным айди сохраняется в папку `test/`
3. `checker.py` компилирует/запускает решение
4. Для каждого теста:
   - Программа получает входные данные из `test/{i}.txt`
   - Результат сравнивается с `ans/{i}.txt`
   - Если `use_checker: true`, результат сначала обрабатывается чекером
5. Подсчитывается количество пройденных тестов
6. Баллы сохраняются в `users_data.json`

## ❓ Часто задаваемые вопросы

**Q: Какое минимальное количество контестов и задач можно создать?**  
A: Можно создать 1 контест с 1 задачей. Главное, чтобы нумерация шла по порядку (contest1, задача A).

**Q: Сколько тестов нужно подготовить для задачи?**  
A: Ровно 100 тестов. Это обязательное требование системы.

**Q: Как добавить кастомный чекер?**  
A: Установите `use_checker: true` в `config.json` и реализуйте логику проверки в `checker.py`. В папке `ans/` храните то, что должен вернуть чекер.

**Q: Что такое чекер и зачем он нужен?**  
A: Чекер позволяет гибко проверять вывод программы. Например, игнорировать лишние пробелы, проверять числа с погрешностью или проверять решения где есть много ответов.

**Q: Как начисляются баллы?**  
A: Каждый пройденный тест дает 1 балл. Максимум 100 баллов за задачу.

**Q: Что происходит при повторной отправке решения?**  
A: Старые баллы перезаписываются новыми, если новое решение показало лучший результат.

**Q: Как сформировать рейтинг участников?**  
A: Войти в контейнер с выключеным сервером запустить скрипт `admin.py` и  ввести команду `top`. Рейтинг сохранится в файле `top.txt`.

**Q: Где хранятся отправленные решения?**  
A: Временно в папке `test/`. После проверки они удаляются.

**Q: Как изменить порт запуска?**  
A: В команде `docker run` измените `-p 5000:5000` на нужный порт, например `-p 8080:5000`.

**Q: Не удаётся подключиться на виндовс**  
A: Самая частая причина брандмауэр блокирует подключение.
Проверьте разрешения на подключеный порт для брандмауэра.

## 📝 Примечания для администраторов

- Скрипт `generator.py` предназначен для ручного использования администраторами. В нем можно реализовать функции для автоматической генерации тестов
- Все данные хранятся в текстовых файлах, рекомендуется иногда делать резервные копии папок `tasks/` и файла `users_data.json`
- Для запуска команд внутри Docker-контейнера используйте `docker exec -it tournament <bash>` после можно выполнять любые нужный команды
- Чекер можно дорабатывать под конкретные задачи, добавляя новую логику проверки в `checker.py`
